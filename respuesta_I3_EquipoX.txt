Santino Ramos Conti
Agostina Pietraroia

a- El máster después de estos cambios contiene la rama fusionada con todos los archivos actualizados.

b- Sí, Beta pudo ver todos los cambios realizados porque se completó el merge de su rama con master.

c- Comandos usados para el merge en el punto 5:
    1. git checkout main
    2. git pull origin main
    3. git merge <tu_apellido>
    4. git push origin main

d- Las ramas creadas por Beta y Gamma no quedan automáticamente actualizadas; es necesario actualizarlas manualmente si desean incluir los últimos cambios del master.

e- Problemas al hacer el merge:
    1. Conflictos por diferencias en archivos compartidos.
    2. Falta de vinculación de la rama master con el remoto (upstream).

f- Comandos usados en el punto 7:
    1. git branch master
    2. git checkout master
    3. git push --set-upstream origin master
    4. git merge <rama_correspondiente>
    5. git push origin master

G- Comandos usados en el punto 8 para completar lo solicitado:
    1. git stash (si había cambios pendientes no confirmados).
    2. git checkout main
    3. git merge master
    4. git push origin main